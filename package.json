{
    "name": "nubank-challenge",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "type": "module",
    "devDependencies": {
        "jest": "^29.7.0",
        "jsdoc": "^4.0.3"
    },
    "scripts": {
        "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --verbose --runInBand test/",
        "test:dev": "NODE_OPTIONS=--experimental-vm-modules npx jest --verbose --runInBand --watchAll test/",
        "test:debug": "node --experimental-vm-modules --inspect-brk node_modules/.bin/jest --runInBand --watchAll test/",
        "generate-docs": "jsdoc -c jsdoc.json"
    }
}
